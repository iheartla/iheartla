#base
hspace
    = ' ' | '\t'
    ;

hspaces
    = {hspace}+
    ;

line
    = '\n' | '\r' | '\f'
    ;

lines
    = {line}+
    ;

identifier
    = !KEYWORDS /[A-Za-z]/
    ;

desc_identifier
    = !KEYWORDS '`' /[A-Za-z][[A-Za-z0-9]*/ '`';


separator
    =
    line {hspaces} ';'
    | ';' {hspaces} line
    | line
    | ';'
    ;
